{
  "name": "ascii-logo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "The Codrops mark as a draggable 3D solid, printed in ASCII on the GPU.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "eslint . && prettier --check .",
    "format": "eslint --fix . && prettier --write ."
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.9.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-simple-import-sort": "^14.0.0",
    "globals": "^17.11.0",
    "prettier": "^3.9.6",
    "vite": "^7.1.3"
  },
  "engines": {
    "node": "^24.15.0"
  }
}
